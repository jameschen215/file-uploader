import { asyncHandler } from '../lib/async-handler.js';

export const handleGetFile = asyncHandler(async (res, req) => {});

export const handleDownloadFile = asyncHandler(async (res, req) => {});

export const handleGetThumbnail = asyncHandler(async (res, req) => {});

export const handleDeleteFile = asyncHandler(async (res, req) => {});
