<div class="flex-grow flex flex-col items-center sm:justify-center gap-5 p-5">
  <form
    id="sign-in-form"
    action="/auth/sign-in"
    data-errors="<%= JSON.stringify(errors) || {} %>"
    method="post"
    class="w-full max-w-md flex flex-col gap-3 sm:px-12 sm:py-14 sm:shadow-md sm:border sm:border-zinc-200 dark:sm:border-zinc-800 sm:rounded-3xl"
    novalidate
  >
    <div class="mb-4">
      <h1 class="text-center text-3xl">Sign in</h1>
    </div>

    <div class="flex flex-col gap-1">
      <label
        for="email"
        class="capitalize text-sm font-medium text-zinc-600 dark:text-zinc-400"
      >
        Email
      </label>
      <input
        type="email"
        id="email"
        name="email"
        value="<%= locals.oldInput?.email || '' %>"
        autocomplete="email"
        required
        aria-describedby="email-error"
        aria-invalid="false"
        class="border border-zinc-300 dark:border-zinc-700 dark:bg-zinc-800 px-4 py-2 outline-none rounded-md dark:focus:bg-zinc-900 focus:border-lime-500 focus:ring-2 focus:ring-lime-50 dark:focus:border-lime-500 dark:focus:ring-lime-950 transition-colors duration-200"
      />

      <div
        id="email-error"
        aria-live="polite"
        class="hidden text-sm text-red-600"
      ></div>
    </div>

    <div class="flex flex-col gap-1">
      <label
        for="password"
        class="capitalize text-sm font-medium text-zinc-600 dark:text-zinc-400"
      >
        Password
      </label>

      <div class="relative">
        <input
          type="password"
          id="password"
          name="password"
          value="<%= locals.oldInput?.password || '' %>"
          autocomplete="new-password"
          required
          aria-describedby="password-error"
          aria-invalid="false"
          class="w-full border border-zinc-300 dark:border-zinc-700 dark:bg-zinc-800 px-4 py-2 outline-none rounded-md dark:focus:bg-zinc-900 focus:border-lime-500 focus:ring-2 focus:ring-lime-50 dark:focus:border-lime-500 dark:focus:ring-lime-950 transition-colors duration-200"
        />

        <!-- toggle password show/hide -->
        <button
          type="button"
          aria-label="Show password."
          class="toggle-psw absolute right-3 top-1/2 -translate-y-1/2 rounded-md text-zinc-500 dark:text-zinc-500 hover:text-zinc-600 dark:hover:text-zinc-400 transition-colors focus-visible:outline-none focus-visible:border-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-zinc-400 dark:focus-visible:ring-offset-zinc-900"
        ></button>
      </div>

      <div
        id="password-error"
        class="hidden text-sm text-red-600"
        aria-live="polite"
      ></div>
    </div>

    <div class="flex flex-col mt-3">
      <button
        type="submit"
        class="w-full bg-lime-600 hover:bg-lime-500 py-2 outline-none text-zinc-50 font-medium rounded-md cursor-pointer transition-colors disabled:opacity-50 disabled:cursor-not-allowed focus-visible:outline-none focus-visible:border-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-zinc-400 dark:focus-visible:ring-offset-zinc-900"
      >
        Sign in
      </button>
    </div>

    <div class="mt-3">
      <p class="text-center text-sm">
        Don't have an account?
        <a
          href="/auth/sign-up"
          class="underline font-semibold focus-visible:outline-none focus-visible:border-none focus-visible:ring-2 focus-visible:ring-zinc-400"
        >
          Sign up
        </a>
        for one now
      </p>
    </div>
  </form>
</div>

<script type="module" src="/scripts/sign-in.js"></script>
