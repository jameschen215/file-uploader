<div class="w-full flex-grow flex flex-col">
  <div class="flex-grow flex flex-col gap-4 px-5 py-5">
    <div
      id="layout-container"
      class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 grid-flow-row gap-5"
    >
      <!-- Files -->
      <% if (locals.files && files.length > 0) { %>
      <!-- loop files -->
      <% files.forEach(file => { %>
      <%- include('partials/layout-item', { category: 'file', item: file }) %>
      <% }) %>
      <% } %>

      <!-- No items -->
      <% if (!locals.files || files.length === 0) { %>
      <div class="col-span-full row-span-full flex flex-col items-center mt-5">
        <span aria-hidden="true">
          <!-- icon -->
          <%- icon({name: 'FolderOpen', size:72, strokeWidth: 1, className: 'text-gray-200 dark:text-gray-800' }) %>
        </span>
        <p class="mt-10">No files found.</p>
      </div>
      <% } %>
    </div>
  </div>
</div>
