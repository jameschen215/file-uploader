<div
  class="flex-1 min-h-0 flex overflow-y-auto scrollbar-thin scrollbar-thumb-zinc-200 dark:scrollbar-thumb-zinc-800 scrollbar-track-transparent"
>
  <div id="layout-container" class="flex-1 flex items-center justify-center">
    <div class="max-w-lg rounded-lg shadow-md p-6 space-y-5">
      <h2 class="text-xl text-center font-semibold text-zinc-700 mb-4">
        Try Different Toast Types
      </h2>

      <button
        id="success-btn"
        class="w-full bg-green-500 hover:bg-green-600 text-white font-semibold py-2 px-4 rounded transition"
      >
        Show Success Toast
      </button>

      <button
        id="error-btn"
        class="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-4 rounded transition"
      >
        Show Error Toast
      </button>

      <button
        id="warning-btn"
        class="w-full bg-yellow-500 hover:bg-yellow-600 text-white font-semibold py-2 px-4 rounded transition"
      >
        Show Warning Toast
      </button>

      <button
        id="info-btn"
        class="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded transition"
      >
        Show Info Toast
      </button>
    </div>
  </div>
</div>

<script type="module" src="/scripts/toast-test.js"></script>
